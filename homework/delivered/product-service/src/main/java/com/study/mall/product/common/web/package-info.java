/**
 * Web 层通用组件（过滤器、拦截器等）。
 *
 * <p>本包启用 JSpecify 空安全：所有引用类型默认非空，可为 null 的成员需显式标注 {@code @Nullable}。
 * 这样重写 Spring（@NullMarked）的方法时，参数的非空契约得以延续，消除
 * “Not annotated parameter overrides @NullMarked” 警告。
 */
@NullMarked
package com.study.mall.product.common.web;

import org.jspecify.annotations.NullMarked;
